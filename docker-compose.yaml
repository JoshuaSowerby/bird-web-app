version: '3.8'
services:
  app:
    image: bird-web-app
    env_file:
      - .env
    ports:
      - "3000:3000"
    depends_on:
      - db
  db:
    image: mariadb
    env_file:
      - .env
    ports:
      - "3306:3306"