version: '3.8'
services:
  app:
    image: bird-web-app
    env_file:
      - .env
    ports:
      - "3000:3000"
  py:
    image: bird-classifier
    env_file:
      - .env
    depends_on:
      - app
